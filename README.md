# `Probabiliity` and `Statistics` in Python

Explore the probabiliity and statistics in python using `Pandas` and `NumPy`.
- Data loading
- Joint probabilities & Conditional probabilities
- Probability distributions
- Model fitting
